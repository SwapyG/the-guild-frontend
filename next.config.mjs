// next.config.mjs

/** @type {import('next').NextConfig} */
const config = {
  // We don't have any custom Next.js config right now,
  // but this file must exist and be in the correct format.
  // We can add things like image remote patterns here later if needed.
};

export default config;