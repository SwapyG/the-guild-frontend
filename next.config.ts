// next.config.ts (Corrected)

import type { NextConfig } from 'next'

const config: NextConfig = {
  // The file might be empty like this, or have other settings.
  // Just ensure the 'experimental' block is gone.
}

export default config